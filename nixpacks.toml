[phases.setup]
nixPkgs = ["python311", "python311Packages.pip", "python311Packages.setuptools"]

[phases.install]
cmds = ["python3.11 -m pip install -e ."]

[start]
cmd = "python3.11 -m uvicorn services.api.main:app --host 0.0.0.0 --port ${PORT:-8000}"
